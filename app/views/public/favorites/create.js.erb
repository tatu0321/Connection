// いいねボタンのカウントを更新
$(`.favorite-count-post-id-<%= @post.id %>`).text('<%= @post.favorites.count %>');

// いいねボタンを❤️に変更
$(`.favorite-post-id-<%= @post.id %>`).replaceWith("<%= j(link_to '❤️', favorite_path(@favorite), method: :delete, remote: true, class: 'text-danger me-2 favorite-post-id-' + @post.id.to_s, data: { id: @post.id }) %>");
